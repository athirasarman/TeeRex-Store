export interface Product {
    id: Number,
    imageURL: string,
    name: string,
    type: string,
    price: Number,
    currency: string,
    color: string,
    gender: string,
    quantity: Number
}
